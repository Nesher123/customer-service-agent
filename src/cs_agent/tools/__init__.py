"""LangChain tools the agent can call to query the Bitext dataset.

The same Pydantic-typed tool functions are reused by the CLI agent and the
FastMCP server (Task 3). See `tools.registry` for the canonical exported list.
"""
