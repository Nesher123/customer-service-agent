"""Streamlit chat UI for the customer-service agent (Bonus A).

The actual page renderer lives in ``streamlit_app``. This package only acts as
an import target so the file can be discovered by ``streamlit run`` (or by the
``cs-agent-ui`` console script that wraps it).
"""
